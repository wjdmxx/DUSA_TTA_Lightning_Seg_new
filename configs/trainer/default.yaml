trainer:
  accelerator: gpu
  devices: 1  # model-parallel inside a single process; use CUDA_VISIBLE_DEVICES=0,1
  precision: bf16-mixed
  max_epochs: 1
  log_every_n_steps: 1
  enable_checkpointing: false
  gradient_clip_val: 1.0
  lr: 1.0e-4
